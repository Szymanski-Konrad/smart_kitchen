abstract class Constants {
  Constants._();

  // Dimensions
  static const imagePreviewHeight = 150.0;
  static const smallImagePreviewHeight = 100.0;
  static const imagePreviewAspectRatio = 1.33;

  // Durations
  static const defaultAnimationDuration = Duration(milliseconds: 500);

  // Sizes
  static const searchTextFieldSize = 48.0;

  // Others
  static const generatedRecipesCount = 2;
}
